import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Acreditate.app',
  appName: 'Acreditate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
