import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Estublantes.app',
  appName: 'Estublantes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
