import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Infaden.app',
  appName: 'Infaden',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
