import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Admitodo.app',
  appName: 'Admitodo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
