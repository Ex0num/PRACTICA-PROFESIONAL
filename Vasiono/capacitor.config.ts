import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Vasiono.app',
  appName: 'Vasiono',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
