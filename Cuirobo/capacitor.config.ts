import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Cuirobo.app',
  appName: 'Cuirobo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
